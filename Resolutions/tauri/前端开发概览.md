# 数据流向

前端不能直接与c++ sidecar通信，必须通过rust作为中转。

数据流向：  
vue页面(TS) -- Tauri API(invoke / Command) -- Rust后端 -- C++ Sidecar

在使用 Sidecar 时，Tauri提供了 `Command` API，允许直接从前端拉起并给C++程序传参、读取其输出。

# 在前端封装 Sidecar 调用

/api/backup.ts